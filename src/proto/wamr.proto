syntax = "proto2";

import "wasm_memory_instance.proto";

package test;

service WAMRService {
    rpc WAMRMethod (WAMRRequest) returns (WAMRMemoryInstance) {}
}

message WAMRRequest {
    required int32 id = 1;
}
